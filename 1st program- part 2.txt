#include <stdio.h>

int main() {
    int n, i, j, time = 0, count = 0;
    
    printf("Enter number of processes: ");
    scanf("%d", &n);

    int pid[n], at[n], bt[n], pr[n], wt[n], tat[n], ct[n], done[n];

    for(i = 0; i < n; i++) {
        pid[i] = i + 1;
        done[i] = 0;
        printf("Enter Arrival Time, Burst Time and Priority for P%d: ", pid[i]);
        scanf("%d %d %d", &at[i], &bt[i], &pr[i]);
    }

    while(count < n) {
        int highest = -1;

        for(i = 0; i < n; i++) {
            if(at[i] <= time && done[i] == 0) {
                if(highest == -1 || pr[i] < pr[highest]) {
                    highest = i;
                }
            }
        }

        if(highest == -1) {
            time++;
        } 
        else {
            wt[highest] = time - at[highest];
            time += bt[highest];
            ct[highest] = time;
            tat[highest] = ct[highest] - at[highest];
            done[highest] = 1;
            count++;
        }
    }

    printf("\nProcess\tAT\tBT\tPriority\tWT\tTAT\n");

    for(i = 0; i < n; i++) {
        printf("P%d\t%d\t%d\t%d\t\t%d\t%d\n", pid[i], at[i], bt[i], pr[i], wt[i], tat[i]);
    }

    return 0;
}






Enter number of processes: 5
Enter Arrival Time, Burst Time and Priority for P1: 0
3
5
Enter Arrival Time, Burst Time and Priority for P2: 2
2
3
Enter Arrival Time, Burst Time and Priority for P3: 3
5
2
Enter Arrival Time, Burst Time and Priority for P4: 4
4
4
Enter Arrival Time, Burst Time and Priority for P5: 6
1
1

Process	AT	BT	Priority	WT	TAT
P1	0	3	5		0	3
P2	2	2	3		7	9
P3	3	5	2		0	5
P4	4	4	4		7	11
P5	6	1	1		2	3











#include <stdio.h>

int main() {
    int n, i, time = 0, completed = 0;
    
    printf("Enter number of processes: ");
    scanf("%d", &n);

    int pid[n], at[n], bt[n], pr[n], rt[n];
    int wt[n], tat[n], ct[n];

    for(i = 0; i < n; i++) {
        pid[i] = i + 1;
        printf("Enter Arrival Time, Burst Time and Priority for P%d: ", pid[i]);
        scanf("%d %d %d", &at[i], &bt[i], &pr[i]);
        rt[i] = bt[i]; // remaining time
    }

    while(completed < n) {
        int highest = -1;

        for(i = 0; i < n; i++) {
            if(at[i] <= time && rt[i] > 0) {
                if(highest == -1 || pr[i] < pr[highest]) {
                    highest = i;
                }
            }
        }

        if(highest == -1) {
            time++;
        } 
        else {
            rt[highest]--; 
            time++;

            if(rt[highest] == 0) {
                completed++;
                ct[highest] = time;
                tat[highest] = ct[highest] - at[highest];
                wt[highest] = tat[highest] - bt[highest];
            }
        }
    }

    printf("\nProcess\tAT\tBT\tPriority\tWT\tTAT\n");

    for(i = 0; i < n; i++) {
        printf("P%d\t%d\t%d\t%d\t\t%d\t%d\n",
               pid[i], at[i], bt[i], pr[i], wt[i], tat[i]);
    }

    return 0;
}









Enter number of processes: 5
Enter Arrival Time, Burst Time and Priority for P1: 0
3
5
Enter Arrival Time, Burst Time and Priority for P2: 2
2
3
Enter Arrival Time, Burst Time and Priority for P3: 3
5
2
Enter Arrival Time, Burst Time and Priority for P4: 4
4
4
Enter Arrival Time, Burst Time and Priority for P5: 6
1
1

Process	AT	BT	Priority	WT	TAT
P1	0	3	5		12	15
P2	2	2	3		6	8
P3	3	5	2		1	6
P4	4	4	4		6	10
P5	6	1	1		0	1













#include <stdio.h>

int main() {
    int n, i, time = 0, remain, tq;

    printf("Enter number of processes: ");
    scanf("%d", &n);

    int pid[n], at[n], bt[n], rt[n];
    int wt[n], tat[n], ct[n];

    for(i = 0; i < n; i++) {
        pid[i] = i + 1;
        printf("Enter Arrival Time and Burst Time for P%d: ", pid[i]);
        scanf("%d %d", &at[i], &bt[i]);
        rt[i] = bt[i];
    }

    printf("Enter Time Quantum: ");
    scanf("%d", &tq);

    remain = n;

    while(remain > 0) {
        for(i = 0; i < n; i++) {
            if(rt[i] > 0 && at[i] <= time) {

                if(rt[i] > tq) {
                    time += tq;
                    rt[i] -= tq;
                } 
                else {
                    time += rt[i];
                    ct[i] = time;
                    rt[i] = 0;
                    tat[i] = ct[i] - at[i];
                    wt[i] = tat[i] - bt[i];
                    remain--;
                }
            }
        }
    }

    printf("\nProcess\tAT\tBT\tWT\tTAT\n");

    for(i = 0; i < n; i++) {
        printf("P%d\t%d\t%d\t%d\t%d\n", pid[i], at[i], bt[i], wt[i], tat[i]);
    }

    return 0;
}






Enter number of processes: 5
Enter Arrival Time and Burst Time for P1: 0
5
Enter Arrival Time and Burst Time for P2: 1
3
Enter Arrival Time and Burst Time for P3: 2
1
Enter Arrival Time and Burst Time for P4: 3
2
Enter Arrival Time and Burst Time for P5: 4
3
Enter Time Quantum: 2

Process	AT	BT	WT	TAT
P1	0	5	9	14
P2	1	3	8	11
P3	2	1	2	3
P4	3	2	2	4
P5	4	3	6	9

